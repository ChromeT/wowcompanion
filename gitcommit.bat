@echo off
git config user.email "chromet@user.com"
git config user.name "ChromeT"
git add .
git commit -m "design: change color palette to Steamvault (Zangarmarsh bioluminescent teal and orange)"
git push origin main
